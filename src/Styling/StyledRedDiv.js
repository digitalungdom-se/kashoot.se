import styled from "styled-components"

const StyledRedDiv = styled.div`
  color: ${props => props.theme.colors.white};
  background-color: ${props => props.theme.colors.mainRed};
`
export default StyledRedDiv
